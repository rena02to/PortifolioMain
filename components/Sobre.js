import style from './../styles/css/Sobre.module.css';

export default function Sobre({ i18n }){
    return(
        <section className={style.sobre} id="sobre">
            <h1>Sobre</h1>
        </section>
    );
}