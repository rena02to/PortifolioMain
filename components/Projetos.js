import style from './../styles/css/Projetos.module.css';

export default function Projetos({ i18n }){
    return(
        <section className={style.projetos} id="projetos">
            <h1>Projetos</h1>
        </section>
    );
}